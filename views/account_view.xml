<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_order_form_inherited" model="ir.ui.view">
        <field name="name">account.move.inherited</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_reference']" position="after">
                <field name="ref" string="Referencia de cliente" attrs="{'invisible: [('hide_val', '=', '1')]}"/>
            </xpath>
        </field>
    </record>

    <!--<record id="view_order_tree_inherited" model="ir.ui.view">
        <field name="name">account.move.inherited</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_out_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='ref']" position="after">
                <field name="payment_reference" string="Referencia de pago"/>
            </xpath>
        </field>
    </record>-->

    <record id="account.action_move_in_invoice_type" model="ir.actions.act_window">
        <field name="name">Potato</field>
        <field name="context">{'default_move_type': 'in_invoice', 'hide_val': '1'}</field>
    </record>

</odoo>